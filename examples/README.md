# examples/

Reference configurations for connecting MCP clients to a running CEGM broker.

## Planned (Phase 1)

- `claude_desktop_config.json` — drops in at `%APPDATA%/Claude/claude_desktop_config.json`
- `cursor_mcp.json` — drops in at `%USERPROFILE%/.cursor/mcp.json` (global) or `<project>/.cursor/mcp.json`
- `claude_code_mcp_add.txt` — the exact `claude mcp add` invocation
- `openai_chat_smoke.py` — minimal OpenAI-compatible client smoke test against the broker's LLM endpoint

## Status

Empty — placeholders only. See [docs/ROADMAP.md](../docs/ROADMAP.md) Phase 1.
